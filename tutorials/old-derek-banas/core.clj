;; Namespace - ns
(ns tutorial.core
  (:require [clojure.string :as str])
  (:gen-class))

(use 'clojure.java.io)

;; defn - define function
;; & args - list of args - & means variable parameter
(defn main [& args]
  ) ;; main

(main)
